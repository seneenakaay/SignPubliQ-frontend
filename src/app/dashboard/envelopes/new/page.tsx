import EnvelopeCreateClient from './EnvelopeCreateClient';

export const metadata = {
  title: 'Create Envelope - SignPubliQ',
  description: 'Upload a document and prepare a new envelope',
};

export default function EnvelopeCreatePage() {
  return <EnvelopeCreateClient />;
}
